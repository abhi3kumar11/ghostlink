server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # GhostLink Backend logs
  - job_name: ghostlink-backend
    static_configs:
      - targets:
          - localhost
        labels:
          job: ghostlink-backend
          __path__: /var/log/ghostlink/backend/*.log

  # GhostLink Frontend/Nginx logs
  - job_name: ghostlink-frontend
    static_configs:
      - targets:
          - localhost
        labels:
          job: ghostlink-frontend
          __path__: /var/log/ghostlink/nginx/*.log

  # System logs
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: system
          __path__: /var/log/host/syslog

  # Docker container logs
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'

